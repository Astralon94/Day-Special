{
  "name": "day-special-server",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "description": "Day Special — planner di matrimonio per due, offline-first: SPA singlefile + server Node (http + node:sqlite) senza dipendenze runtime, sync multi-dispositivo via SSE.",
  "scripts": {
    "start": "node server.js",
    "build": "vite build && cp dist/index.html public/index.html",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^7.3.6",
    "vite-plugin-singlefile": "^2.0.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
